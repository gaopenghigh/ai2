# Day 06 · 我的笔记

## 三个必须记住的数（我这台机器）

| | Mac mini M4 | RTX 4050 |
|---|---|---|
| 一个 kernel 的过路费（实验 A ④ 的 sync 列） |  µs |  µs |
| 一次同步的代价（实验 B 队列深度表第 1 行） |  µs |  µs |
| 稳态单价（队列深度表最后一行） |  µs |  µs |

**和 Day 04 实测的 $t_0$ 对得上吗？** ______

---

## 实验 A · 开销阶梯

| 这一步做了什么 | 不 sync（CPU 提交） | sync（端到端） |
|---|---|---|
| ① 纯 Python 空函数 |  |  |
| ② 读元数据 x.shape |  |  |
| ③ 元数据操作 x.view(1) |  |  |
| **④ 1 个元素相加** |  |  |
| ⑤ 1024² 相加 |  |  |
| ⑥ 4096² 相加 |  |  |

**③→④ 跳了 ___ 倍。** 这一跳就是软件栈的过路费。

---

## 实验 B · 异步

```
不 sync           µs
sync              µs
比值              ×      ← 这个数说明了什么：
```

**队列深度**

| 连发个数 | 1 | 10 | 100 | 1000 |
|---|---|---|---|---|
| 平均每个 (µs) |  |  |  |  |

**隐式同步**：加 `.item()` 后从 ___ µs 变成 ___ µs，慢了 ___ 倍

**我在自己以前的代码里找到的隐式同步**：
-
-

---

## 实验 D · 一层 Transformer 的账

| batch | 每步耗时 | 有效算力 | 算力达成率 | 权重搬运占比 |
|---|---|---|---|---|
| 1 |  |  |  |  |
| 16 |  |  |  |  |
| 256 |  |  |  |  |

**一层调用了 ___ 次 aten 算子**

**外推表（软件开销占比）**

| 机器 | M4 | 4050 | 4090 | A100 | H100 |
|---|---|---|---|---|---|
| 占比 |  |  |  |  |  |

**把 d 从 4096 改成 1024 后，占比变成了：** ______　**为什么：** ______

---

## 实验 E · profiler（重点在 4050 上跑）

```
Self CPU 总和        =        µs
Self Device 总和     =        µs
判定：           -bound
```

**排名前三的算子**：
1.
2.
3.

**可疑条目（aten::empty / to / copy_ 之类）占比**：______

---

## 手算练习

**Q1** kernel 执行 3 µs、发射 5 µs、800 个
```
总时间 = 800 ×          =        µs
GPU 利用率 =        %
CUDA Graph 后 =        µs，提速      ×
```

**Q2** 7B decode 的发射开销占比
```
发射开销 = 32 × 51 × 5 µs =        ms
4050：权重搬运        ms  →  占比      %
H100：权重搬运        ms  →  占比      %
```

**Q3** prefill 为什么不需要 CUDA Graph
```
发射次数：decode        ，prefill        （一样吗？    ）
GPU 时间：decode        ，prefill
占比：decode      %，prefill      %
```

**Q4** batch = B 时，每 token 的发射开销 = ____________

---

## 我的疑问 / 想深挖的点

-
-

---

## 一句话总结今天

>
