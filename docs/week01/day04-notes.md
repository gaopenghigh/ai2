# Day 04 · 我的笔记

## 实验 A · 工作集扫描（今日重点）

从曲线上直接读出的三个硬件参数：

| 参数 | Mac mini M4 | RTX 4050 |
|---|---|---|
| kernel 启动开销 $t_0$ |  µs |  µs |
| 稳态 DRAM 带宽 |  GB/s |  GB/s |
| 缓存段的最高有效带宽 |  GB/s |  GB/s |
| **缓存拐点（工作集）** |  MB |  MB |
| 拐点 / 官方 L2 容量 | Apple 不公布 | TechPowerUp 查到： MB |

原始数据（把 `S/(t−t0)` 那一列抄下来）：

```
工作集      耗时        S/t        S/(t-t0)     段
4 KB
256 KB
1 MB
2 MB
4 MB
8 MB
16 MB
64 MB
256 MB
```

**我的判断：这台机器在 ___ MB 处越过了缓存，所以那一级缓存大约 ___ MB。**

---

## 实验 B · 局部性（交叉验证）

| 源数组大小 | 随机 / 顺序（M4） | 随机 / 顺序（4050） |
|---|---|---|
| 0.5 MB |  |  |
| 2 MB |  |  |
| 8 MB |  |  |
| 32 MB |  |  |
| 256 MB |  |  |

**比值开始起飞的位置 = ___ MB。和实验 A 的拐点对上了吗？**

---

## 实验 C · 跨过那道墙

| 方向 / 方式 | M4 GB/s | 4050 GB/s |
|---|---|---|
| 显存 → 显存（读+写） |  |  |
| 主机 → GPU（pageable） |  |  |
| 主机 → GPU（**pinned**） | 不适用 |  |
| GPU → 主机 |  |  |

**关键比值**：`显存带宽 ÷ 主机↔GPU 带宽 = ______`

**这个比值意味着**：offload 的比例必须远小于 1/______ 才不亏

---

## 实验 D · 融合

| 实现 | 显存流量 | 耗时 | 等效带宽 |
|---|---|---|---|
| eager（4 个 kernel） | 512 MB |  ms |  GB/s |
| torch.compile（1 个） | 128 MB |  ms |  GB/s |

**两行的等效带宽是否几乎相同？** ______
**加速比 = ______ ×，和流量比 4× 接近吗？** ______

---

## 手算练习

**Q1** 从三个点反推
```
B  = (128−64) MB / (1380−700) µs =         GB/s
t0 = 1380 µs −            =                µs
1 MB 点：模型预测       µs，实测 12 µs  →  结论：
```

**Q2** 13B INT4，20% 层放 CPU
```
假如显存够大 = 6.5 / 192 =            ms
实际         =        +        =      ms
慢了                倍；其中 PCIe 那一项占了      %
```

**Q3** 上下文 4096 → 16384
```
朴素 attention 流量涨      倍（因为 O(    )）
FlashAttention 流量涨      倍（因为 O(    )）
```

**Q4** 128×128 tile 要多少 Shared Memory
```
每轮放进 SMEM =        +        =        KB
双缓冲 ×2     =        KB
100 KB 能放   =        个 block
```

---

## 我的疑问 / 想深挖的点

-
-

---

## 一句话总结今天

>
