# Day 05 · 我的笔记

## 三个必须记住的数（FP16）

```
天花板  =            （超过就是 inf）
地板    =            （小于就是 0）
数不动  =            （从 0 一个一个加 1，停在这里）
```

## 手拆位串

`0 10001 1001000000`（FP16）
```
S =      E 域 =        实际指数 =        M 域 =
1.M =                值 =
```

`0 00000 0000000001`（FP16）
```
指数域全 0 → 走       公式（有没有隐含的 1？    ）
值 =
```

---

## 实验 B · 边界表（手推 vs torch.finfo）

| 格式 | max | 最小正规数 | 最小次正规数 | eps | 十进制有效位 |
|---|---|---|---|---|---|
| FP32 |  |  |  |  |  |
| BF16 |  |  |  |  |  |
| FP16 |  |  |  |  |  |
| FP8 E4M3 |  |  |  |  |  |
| FP8 E5M2 |  |  |  |  |  |

**对上了吗？** ______

---

## 实验 C · 三种翻车方式（今日重点）

把 65536 个 1 加起来（正确答案 65536）：

| 求和方式 | 我的结果 | 栽在哪 |
|---|---|---|
| 朴素逐个累加（FP16） |  |  |
| `torch.sum()`（FP16） |  |  |
| `torch.sum(dtype=float32)` |  |  |

**停住的位置**：FP16 = ______（手推 $2^{11}$ = ______）　BF16 = ______（手推 $2^8$ = ______）

---

## 实验 D · 找最优 loss scale

| 做法 | 下溢成 0 | 溢出成 inf | 平均相对误差 |
|---|---|---|---|
| 直接转 FP16 |  |  |  |
| 直接转 BF16 |  |  |  |
| FP16 + ×2^12 |  |  |  |
| FP16 + ×2^16 |  |  |  |
| FP16 + ×2^24 |  |  |  |
| FP16 + ×2^28 |  |  |  |

**我这个分布下的最优 scale = 2^______**
**窗口宽度（不下溢也不溢出的 scale 范围）= 2^____ ~ 2^____**

---

## 实验 E · 量化

矩阵乘输出的相对误差：

| 方案 | 干净权重 | 注入离群值后 |
|---|---|---|
| INT8 per-tensor |  |  |
| INT8 per-group 128 |  |  |
| INT4 per-tensor |  |  |
| INT4 per-group 128 |  |  |
| INT4 per-group 32 |  |  |

**离群值倍数改成 5×，per-tensor 从什么时候开始崩？** ______
**改成 100× 呢？** ______

---

## 手算练习

**Q1** `0 10001 1001000000` = ______　`0 00000 0000000001` = ______

**Q2** BF16 存 65504 会得到 ______，因为 ______

**Q3** 7B 全量微调（BF16 + Adam）
```
2(权重) + 2(梯度) + 4(FP32主副本) + 4(m) + 4(v) =      字节/参数
7e9 ×      =        GB
4050(6GB) 能跑吗：       H100(80GB) 能跑吗：
```

**Q4** INT4 group 128 对称量化
```
每组字节 = 128×0.5 +   =
每参数 bit =           相对 FP16 压缩      ×
group 32 时：每参数 bit =           压缩      ×
```

---

## 我的疑问 / 想深挖的点

-
-

---

## 一句话总结今天

>
