# X02 · 我的笔记

## 三条必须背下来的规则

```
① 看到形状先问：这是【            】还是【            】？

② 最后一维 = ____________，前面所有维度 = ____________

③ 口诀：____________，____________，____________
```

**算术强度公式**：$\text{AI} = \dfrac{2Bdk}{(\,\_\_\_ + \_\_\_ + \_\_\_\,)\cdot b} \approx$ ________

---

## 实验 A · 三种读法

| 读法 | 一句话 | 用在哪 | 能不能复用数据 |
|---|---|---|---|
| ① 行 × 列 |  |  |  |
| ② 列的线性组合 |  |  |  |
| ③ 外积求和 |  |  |  |

**为什么硬件必须用第三种**：______________

**它对应 Day 03 的哪一段**：______________

---

## 实验 B · 形状追踪器

把输出抄下来（用你自己改的 T/d/h）：

```
T = ____   d = ____   h = ____   dh = ____

1  输入                 →
2  x @ Wq               →        消掉了
5  拆头                 →
6  Q @ K.T              →        消掉了
9  @ V                  →        消掉了
11 @ Wo                 →
```

**第 1 步和第 11 步形状一样吗？** ______　**这为什么重要？** ______________

**把 d 改成不能被 h 整除时，报的是什么错**：______________

---

## 实验 C · nn.Linear

```
nn.Linear(in=___, out=___).weight.shape = (____, ____)
lin(x) == x @ ______ + ______     误差 =
```

**为什么存 (out, in)**：
- 理由①（数学习惯）：
- 理由②（访存）：

---

## 实验 D · 转置的代价

| 写法 | 耗时 | 相对 |
|---|---|---|
| `x @ w.T` |  |  |
| `x @ w_t` |  |  |
| `x @ w` |  | 1.00× |
| `w.T.contiguous()` |  | — |

**`.contiguous()` 的等效带宽 = ______ GB/s，而峰值是 ______ GB/s**

**为什么只有一半**：______________

**把 n 改成 512 后，三种写法的差距变大了吗？为什么**：______________

---

## 实验 E · AI ≈ batch size（重点）

| batch B | 1 | 8 | 32 | 128 | 512 | 2048 |
|---|---|---|---|---|---|---|
| 搬运字节 |  |  |  |  |  |  |
| 算术强度 |  |  |  |  |  |  |
| 实测 TFLOPS |  |  |  |  |  |  |
| 哪一侧 |  |  |  |  |  |  |

**B 涨了 ______ 倍，搬运字节只涨了 ______ 倍**

**换成 fp32 后，AI 变成了 ______**（验证公式里的 $b$）

---

## 手算练习

**Q1** 形状推演（B=2, T=8, d=512, h=8）
```
x                     →
x @ Wq                →
.view(B,T,h,d//h)     →
.transpose(1,2)       →
q @ k.transpose(-2,-1)→
att @ v               →
.transpose(1,2).reshape →
```
**这道题的陷阱在哪**：______________

**Q2** 每层参数量
```
注意力 4 个矩阵 =        FFN 两个矩阵 =        合计 =      d²
d=4096 → 每层        M   ×32 层 =        B
+ embedding        M  →  总共约      B
```

**Q3** FFN 的算术强度
```
B=1 时 AI =
要 AI ≥ 125，B ≥
那时激活占        MB   →  结论：限制 batch 的其实是
```

**Q4** 广播
```
a(8,512) + b(512)  →
a(8,512) + c(8)    →
a(8,512) + d(8,1)  →
d(8,1)   + b(512)  →        ← 最危险的一行，因为
```

---

## 思考题（先写自己的答案）

**T1** INT4 权重时 $b$ 到底算多少？B=32 真能撞算力墙吗？
>

**T2** 为什么不把 K 切成 1 列一段？
>

**T3** KV Cache 存 `(B,h,T,dh)` 还是 `(B,T,h,dh)`？
>

---

## 我的疑问 / 想深挖的点

-
-

---

## 一句话总结今天

>
